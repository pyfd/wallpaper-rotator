# Allow the login-background refresh to run at login without a password prompt.
# @@USER@@ is substituted by install.sh. Installed to /etc/sudoers.d/random-login-bg (mode 0440).
@@USER@@ ALL=(root) NOPASSWD: /usr/local/bin/random-login-bg.sh
